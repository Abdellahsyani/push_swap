#include <stdio.h>


int	main(int ac, char *av)
{
	int	*stack_a;
	int	*stack_b;
	int	i = 0;
	int 	j = 0;

	if (ac > 1)
	{
		int	len = strlen(ac);
		int	pivot = av[0];
		stack_a = malloc(sizeof(len));
		while (av[i])
		{
		}
	}
}
